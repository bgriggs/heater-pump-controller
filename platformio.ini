; PlatformIO build config. The sketch stays in heater-controller/ so it still
; opens in the Arduino IDE.
[platformio]
src_dir = heater-controller

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
